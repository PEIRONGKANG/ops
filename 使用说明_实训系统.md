# 饮品实训周运营系统使用说明（工程版）

## 1. 推荐启动方式

请优先双击：

- `启动工程版实训系统.bat`

如果你更习惯英文文件名，也可以双击：

- `start_ops_training_system.bat`

脚本会自动完成：

- 检查或安装 Python 3.11
- 创建本地 `.venv`
- 离线安装后端依赖
- 直接使用已构建好的前端页面
- 启动系统并自动打开浏览器

启动地址固定为：

- [http://127.0.0.1:8000](http://127.0.0.1:8000)

## 2. 如果换到另一台 Windows 电脑

最稳的做法不是只发源码，而是直接复制整个项目文件夹。

请确保这些内容一起带走：

- `.vendor`
- `backend`
- `frontend/dist`
- `assets`
- `启动工程版实训系统.bat`
- `start_ops_training_system.bat`

复制过去后，直接双击 `启动工程版实训系统.bat` 即可。

## 3. 数据保存位置

工程版不再依赖浏览器 `localStorage`，而是使用 SQLite。

数据库文件位置：

- `backend/data/ops_training.db`

里面保存了：

- 账号
- 分组
- 每周运营记录
- 经理确认状态

## 4. 如何把数据从一台电脑迁到另一台电脑

### 方法一：直接复制数据库

1. 先关闭两台电脑上的系统。
2. 从源电脑复制：
   `backend/data/ops_training.db`
3. 粘贴到目标电脑同一路径并覆盖原文件。
4. 再启动系统。

### 方法二：用脚本备份和恢复

源电脑：

1. 双击 `备份工程版数据库.bat`
2. 在 `database-backups/` 里找到新生成的 `.db` 备份
3. 把这个备份文件拷到目标电脑

目标电脑：

1. 双击 `恢复工程版数据库.bat`
2. 把备份 `.db` 文件拖到该脚本上
3. 恢复完成后重新启动系统

## 5. 日常使用建议

- 迁移数据库前先关闭系统，避免写入冲突。
- 如果目标电脑已有数据，先执行一次 `备份工程版数据库.bat`。
- 想保留完整历史，最关键的是保留 `backend/data/ops_training.db`。

## 6. 旧版说明

如果只是临时查看旧原型，可以继续打开：

- `weekly_ops_system.html`

但正式使用、数据保存、账号管理、导出和迁移，都请以工程版系统为准。
